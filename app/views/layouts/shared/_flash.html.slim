- flash.each do |name, msg|
  = content_tag :div, class: (name == "notice" ? "alert alert-dismissable alert-success" : "alert alert-dismissable alert-danger") do
    button type="button" class="close" data-dismiss="alert" ×
    = msg
